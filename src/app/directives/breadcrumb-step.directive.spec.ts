import { BreadcrumbStepDirective } from './breadcrumb-step.directive';

describe('BreadcrumbStepDirective', () => {
  it('should create an instance', () => {
    const directive = new BreadcrumbStepDirective();
    expect(directive).toBeTruthy();
  });
});
